{
  "name": "rest-api-sqlite",
  "version": "1.0.0",
  "description": "A RESTful API connected to a database with SQLite",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "fkolender",
  "license": "ISC",
  "dependencies": {
    "sqlite3": "^5.1.6"
  }
}
